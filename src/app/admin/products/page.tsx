"use client";
/* eslint-disable @next/next/no-img-element */

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { products as seedProducts, Product, formatBaht } from "@/lib/data";
import MultipleImageUploader from "@/components/admin/multiple-image-uploader";

type Category = { id: string; name: string; _count?: { products: number } };
type ProductStatus = "AVAILABLE" | "DISCONTINUED";
type ProductDraft = Product & { images: string[]; status: ProductStatus };
type ProductEdit = { name: string; sku: string; category: string; price: string; stock: string; description: string; status: ProductStatus };
const categoryKey = "tamiya_categories";
const productKey = "tamiya_admin_products";
const defaultCategories: Category[] = [{ id: "cars", name: "รถ Mini 4WD" }, { id: "parts", name: "อะไหล่และมอเตอร์" }, { id: "tools", name: "อุปกรณ์แต่งรถ" }];

export default function ProductManagement() {
  const [categories, setCategories] = useState<Category[]>(defaultCategories);
  const [items, setItems] = useState<ProductDraft[]>([]);
  const [newCategory, setNewCategory] = useState("");
  const [categoryEditorOpen, setCategoryEditorOpen] = useState(false);
  const [selectedImages, setSelectedImages] = useState<string[]>([]);
  const [selectedFiles, setSelectedFiles] = useState<File[]>([]);
  const [message, setMessage] = useState("");
  const [searchSku, setSearchSku] = useState("");
  const [searchName, setSearchName] = useState("");
  const [searchCategory, setSearchCategory] = useState("");
  const [selectedProduct, setSelectedProduct] = useState<ProductDraft | null>(null);
  const [editingStatus, setEditingStatus] = useState<ProductStatus>("AVAILABLE");
  const [editingProduct, setEditingProduct] = useState<ProductEdit | null>(null);
  const [form, setForm] = useState({ name: "", sku: "", category: defaultCategories[0].name, price: "", stock: "", description: "" });

  useEffect(() => {
    let active = true;
    Promise.all([fetch("/api/admin/categories"), fetch("/api/admin/products")]).then(async ([categoryResponse, productResponse]) => {
      if (!categoryResponse.ok || !productResponse.ok) throw new Error("API unavailable");
      const apiCategories = await categoryResponse.json() as Category[];
      const apiProducts = await productResponse.json() as Array<Product & { status?: string; category: Category; images: Array<{ url: string }> }>;
      if (!active) return;
      setCategories(apiCategories);
      setItems(apiProducts.map((product) => ({ ...product, status: product.status === "DISCONTINUED" ? "DISCONTINUED" : "AVAILABLE", category: product.category?.name || "", images: product.images.map((image) => image.url) })));
      setForm((current) => ({ ...current, category: apiCategories[0]?.name || current.category }));
    }).catch(() => {
      const savedCategories = window.localStorage.getItem(categoryKey);
      const savedProducts = window.localStorage.getItem(productKey);
      if (savedCategories) setCategories(JSON.parse(savedCategories));
      if (savedProducts) setItems(JSON.parse(savedProducts));
      else setItems(seedProducts.map((product) => ({ ...product, images: [], status: "AVAILABLE" as ProductStatus })));
      setMessage("API ยังไม่พร้อม ใช้ข้อมูล Demo ชั่วคราว");
    });
    return () => { active = false; };
  }, []);

  const saveCategories = (next: Category[]) => { setCategories(next); window.localStorage.setItem(categoryKey, JSON.stringify(next)); };
  const addCategory = async () => { const name = newCategory.trim(); if (!name) return; const response = await fetch("/api/admin/categories", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name }) }); if (!response.ok) { setMessage("เพิ่ม Category ไม่สำเร็จ หรือมีชื่อซ้ำ"); return; } const created = await response.json() as Category; const next = [...categories, created]; saveCategories(next); setForm((current) => ({ ...current, category: name })); setNewCategory(""); setCategoryEditorOpen(false); setMessage(`เพิ่ม Category “${name}” แล้ว`); };
  const readImages = (files: File[]) => { setSelectedFiles((current) => [...current, ...files]); Promise.all(files.map((file) => new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = reject; reader.readAsDataURL(file); }))).then((images) => setSelectedImages((current) => [...current, ...images])); };
  const addProduct = async (event: FormEvent) => { event.preventDefault(); if (!form.name.trim() || !form.sku.trim() || !form.price || !form.stock) { setMessage("กรอกชื่อสินค้า รหัส ราคา และสต็อกให้ครบ"); return; } setMessage("กำลังอัปโหลดรูปและบันทึกสินค้า..."); let imageUrls: string[] = []; if (selectedFiles.length) { const uploadData = new FormData(); selectedFiles.forEach((file) => uploadData.append("images", file)); const uploadResponse = await fetch("/api/admin/upload", { method: "POST", body: uploadData }); if (!uploadResponse.ok) { setMessage("อัปโหลดรูปไม่สำเร็จ"); return; } imageUrls = (await uploadResponse.json()).urls; } const category = categories.find((item) => item.name === form.category); if (!category) { setMessage("กรุณาเลือก Category"); return; } const response = await fetch("/api/admin/products", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ sku: form.sku, name: form.name, categoryId: category.id, price: Number(form.price), stock: Number(form.stock), description: form.description, images: imageUrls }) }); if (!response.ok) { setMessage("บันทึกสินค้าไม่สำเร็จ หรือ SKU ซ้ำ"); return; } const saved = await response.json(); setItems((current) => [{ ...saved, status: saved.status === "DISCONTINUED" ? "DISCONTINUED" : "AVAILABLE", category: form.category, images: imageUrls }, ...current]); setForm({ name: "", sku: "", category: categories[0]?.name || "", price: "", stock: "", description: "" }); setSelectedImages([]); setSelectedFiles([]); setMessage(`เพิ่มสินค้า “${saved.name}” แล้ว`); };
  const updateProduct = async () => { if (!selectedProduct || !editingProduct) return; const category = categories.find((item) => item.name === editingProduct.category); if (!category || !editingProduct.name.trim() || !editingProduct.sku.trim() || !editingProduct.price || !editingProduct.stock) { setMessage("กรอกข้อมูลสินค้าให้ครบ"); return; } const response = await fetch("/api/admin/products", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ id: selectedProduct.id, name: editingProduct.name, sku: editingProduct.sku, categoryId: category.id, price: Number(editingProduct.price), stock: Number(editingProduct.stock), description: editingProduct.description, status: editingProduct.status }) }); if (!response.ok) { setMessage("แก้ไขสินค้าไม่สำเร็จ หรือ SKU ซ้ำ"); return; } const saved = await response.json(); const normalized = { ...saved, category: category.name, images: saved.images.map((image: { url: string }) => image.url), status: saved.status as ProductStatus }; setItems((current) => current.map((item) => item.id === selectedProduct.id ? normalized : item)); setSelectedProduct(normalized); setEditingProduct({ ...editingProduct, status: normalized.status }); setMessage(`แก้ไขสินค้า “${normalized.name}” แล้ว`); };
  const filteredItems = items.filter((item) => { const sku = searchSku.trim().toLowerCase(); const name = searchName.trim().toLowerCase(); return (!sku || item.sku.toLowerCase().includes(sku)) && (!name || item.name.toLowerCase().includes(name)) && (!searchCategory || item.category === searchCategory); });
  return <><main className="min-h-screen bg-white"><aside className="fixed hidden h-screen w-64 bg-[#18212b] p-7 text-white md:block"><Link href="/" className="text-xl font-black">TAMIYA<span className="text-[#ef6c3d]">.</span></Link><p className="mt-2 text-xs text-white/45">PREMIUM SHOP CMS</p><nav className="mt-12 grid gap-3 text-sm"><Link href="/admin" className="p-3 text-white/60">Overview</Link><Link href="/admin/products" className="rounded-xl bg-white/10 p-3 font-bold">สินค้า</Link><Link href="/admin/categories" className="p-3 text-white/60">จัดการ Category</Link><span className="p-3 text-white/35">ออเดอร์</span><span className="p-3 text-white/35">ลูกค้า</span></nav></aside><section className="md:ml-64"><header className="flex items-center justify-between border-b bg-white px-6 py-5 md:px-10"><div><p className="text-xs font-bold uppercase tracking-widest text-[#ef6c3d]">Catalog</p><h1 className="mt-1 text-2xl font-black">จัดการสินค้า</h1></div><Link href="/admin" className="rounded-full border px-4 py-2 text-sm font-bold">← Dashboard</Link></header><div className="grid gap-6 p-6 md:p-10 xl:grid-cols-[1fr_1.4fr]"> <div className="space-y-6"><section className="rounded-3xl border border-[#e8ebee] bg-white p-6 shadow-sm"><div className="mb-5"><p className="eyebrow">New product</p><h2 className="mt-1 text-xl font-black">เพิ่มสินค้า</h2></div><form onSubmit={addProduct} className="grid gap-4"><input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="ชื่อสินค้า" className="rounded-xl border p-3 text-sm"/><div className="grid gap-4 sm:grid-cols-2"><input required value={form.sku} onChange={(event) => setForm({ ...form, sku: event.target.value })} placeholder="รหัสสินค้า เช่น 15437" className="rounded-xl border p-3 text-sm"/><div className="flex gap-2"><select value={form.category} onChange={(event) => setForm({ ...form, category: event.target.value })} className="min-w-0 flex-1 rounded-xl border p-3 text-sm">{categories.map((category) => <option key={category.id}>{category.name}</option>)}</select><button type="button" onClick={() => setCategoryEditorOpen((open) => !open)} className="rounded-xl border px-3 text-sm font-bold">+ Category</button></div></div><div className="grid gap-4 sm:grid-cols-2"><input required type="number" min="0" value={form.price} onChange={(event) => setForm({ ...form, price: event.target.value })} placeholder="ราคา (บาท)" className="rounded-xl border p-3 text-sm"/><input required type="number" min="0" value={form.stock} onChange={(event) => setForm({ ...form, stock: event.target.value })} placeholder="จำนวนสต็อก" className="rounded-xl border p-3 text-sm"/></div><textarea value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} placeholder="รายละเอียดสินค้า" rows={3} className="rounded-xl border p-3 text-sm"/><div><label htmlFor="product-images" className="mb-2 block text-sm font-bold">รูปสินค้า (เลือกได้หลายรูป)</label><MultipleImageUploader onFilesSelected={readImages}/><p className="mt-2 text-xs text-[#687582]">รองรับ JPG, PNG, WebP · รูปจะถูก Preview ก่อนบันทึก</p></div>{selectedImages.length > 0 && <div className="grid grid-cols-4 gap-2">{selectedImages.map((image, index) => <div key={`${image.slice(0, 20)}-${index}`} className="relative aspect-square overflow-hidden rounded-xl bg-[#f5f7f8]"><img src={image} alt={`รูปสินค้า ${index + 1}`} className="h-full w-full object-cover"/><button type="button" onClick={() => { setSelectedImages(selectedImages.filter((_, imageIndex) => imageIndex !== index)); setSelectedFiles(selectedFiles.filter((_, fileIndex) => fileIndex !== index)); }} className="absolute right-1 top-1 h-6 w-6 rounded-full bg-white font-bold shadow" aria-label="ลบรูป">×</button></div>)}</div>}<button className="rounded-full bg-[#ef6c3d] px-5 py-3 font-black text-white">บันทึกสินค้า</button>{message && <p className="rounded-xl bg-[#f5f7f8] p-3 text-sm font-bold">{message}</p>}</form></section></div><section className="h-fit rounded-3xl border border-[#e8ebee] bg-white p-6 shadow-sm"><div className="mb-4 flex items-end justify-between"><div><p className="eyebrow">Product catalog</p><h2 className="mt-1 text-xl font-black">สินค้าทั้งหมด ({filteredItems.length}{(searchSku || searchName || searchCategory) ? ` / ${items.length}` : ""})</h2></div><span className="text-xs text-[#687582]">Prisma + DigitalOcean Spaces</span></div><div className="mb-5 grid gap-3 md:grid-cols-[1fr_1fr_1fr_auto]"><input value={searchSku} onChange={(event) => setSearchSku(event.target.value)} placeholder="ค้นหา SKU" className="rounded-xl border p-3 text-sm"/><input value={searchName} onChange={(event) => setSearchName(event.target.value)} placeholder="ค้นหาชื่อสินค้า" className="rounded-xl border p-3 text-sm"/><select value={searchCategory} onChange={(event) => setSearchCategory(event.target.value)} className="rounded-xl border p-3 text-sm"><option value="">ทุก Category</option>{categories.map((category) => <option key={category.id} value={category.name}>{category.name}</option>)}</select><button type="button" onClick={() => { setSearchSku(""); setSearchName(""); setSearchCategory(""); }} className="rounded-xl border px-4 text-sm font-bold">ล้าง</button></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="border-b text-xs text-[#687582]"><tr><th className="pb-3">สินค้า</th><th className="pb-3">SKU</th><th className="pb-3">Category</th><th className="pb-3">ราคา</th><th className="pb-3">คงเหลือ</th><th className="pb-3">สถานะ</th><th className="pb-3">รูป</th></tr></thead><tbody>{filteredItems.map((item) => <tr key={item.id} onClick={() => { setSelectedProduct(item); setEditingStatus(item.status); setEditingProduct({ name: item.name, sku: item.sku, category: item.category, price: String(item.price), stock: String(item.stock), description: item.description, status: item.status }); }} onKeyDown={(event) => { if (event.key === "Enter" || event.key === " ") setSelectedProduct(item); }} tabIndex={0} className="cursor-pointer border-b transition hover:bg-[#fafbfc] focus:bg-[#fafbfc] last:border-0"><td className="py-4 font-bold">{item.name}</td><td className="font-mono text-xs">{item.sku}</td><td>{item.category}</td><td>{formatBaht(item.price)}</td><td className={item.stock <= 0 ? "font-bold text-red-600" : "font-bold"}>{item.stock}</td><td><span className={`rounded-full px-2 py-1 text-xs font-bold ${item.status === "AVAILABLE" ? "bg-green-50 text-green-700" : "bg-gray-100 text-gray-600"}`}>{item.status === "AVAILABLE" ? "พร้อมจำหน่าย" : "ยกเลิกจำหน่าย"}</span></td><td>{item.images.length > 0 ? `${item.images.length} รูป` : "ยังไม่มี"}</td></tr>)}</tbody></table></div></section></div></section></main>{categoryEditorOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#18212b]/45 p-6" role="dialog" aria-modal="true" aria-labelledby="category-modal-title"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="eyebrow">New category</p><h2 id="category-modal-title" className="mt-1 text-xl font-black">เพิ่ม Category</h2></div><button type="button" onClick={() => setCategoryEditorOpen(false)} className="text-2xl leading-none text-[#687582]" aria-label="ปิดหน้าต่าง">×</button></div><input autoFocus value={newCategory} onChange={(event) => setNewCategory(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") addCategory(); }} placeholder="ชื่อ Category ใหม่" className="mt-6 w-full rounded-xl border p-3 text-sm"/><div className="mt-5 flex justify-end gap-2"><button type="button" onClick={() => setCategoryEditorOpen(false)} className="rounded-full border px-4 py-2 text-sm font-bold">ยกเลิก</button><button type="button" onClick={() => addCategory()} className="rounded-full bg-[#ef6c3d] px-4 py-2 text-sm font-bold text-white">เพิ่ม Category</button></div></div></div>}{selectedProduct && <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#18212b]/45 p-6" role="dialog" aria-modal="true" aria-labelledby="product-detail-title" onClick={() => setSelectedProduct(null)}><div onClick={(event) => event.stopPropagation()} className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-3xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between"><div><p className="eyebrow">Product detail</p><h2 id="product-detail-title" className="mt-1 text-2xl font-black">{selectedProduct.name}</h2></div><button type="button" onClick={() => setSelectedProduct(null)} className="text-2xl leading-none text-[#687582]" aria-label="ปิดรายละเอียด">×</button></div><div className="mt-6 grid gap-6 md:grid-cols-[1.1fr_1fr]"><div>{selectedProduct.images.length > 0 ? <div className="grid grid-cols-2 gap-3">{selectedProduct.images.map((image, index) => <img key={`${image}-${index}`} src={image} alt={`${selectedProduct.name} รูปที่ ${index + 1}`} className="aspect-square w-full rounded-2xl object-cover"/>)}</div> : <div className="flex aspect-square items-center justify-center rounded-2xl bg-[#f5f7f8] text-sm text-[#687582]">ยังไม่มีรูปสินค้า</div>}</div><div className="space-y-4 text-sm">{editingProduct && <div className="space-y-3 rounded-2xl border border-[#e8ebee] p-4"><p className="font-bold">แก้ไขข้อมูลสินค้า</p><input value={editingProduct.name} onChange={(event) => setEditingProduct({ ...editingProduct, name: event.target.value })} placeholder="ชื่อสินค้า" className="w-full rounded-xl border p-3 text-sm"/><div className="grid gap-3 sm:grid-cols-2"><input value={editingProduct.sku} onChange={(event) => setEditingProduct({ ...editingProduct, sku: event.target.value })} placeholder="SKU" className="rounded-xl border p-3 text-sm"/><select value={editingProduct.category} onChange={(event) => setEditingProduct({ ...editingProduct, category: event.target.value })} className="rounded-xl border p-3 text-sm">{categories.map((category) => <option key={category.id}>{category.name}</option>)}</select><input type="number" min="0" value={editingProduct.price} onChange={(event) => setEditingProduct({ ...editingProduct, price: event.target.value })} placeholder="ราคา" className="rounded-xl border p-3 text-sm"/><input type="number" min="0" value={editingProduct.stock} onChange={(event) => setEditingProduct({ ...editingProduct, stock: event.target.value })} placeholder="จำนวนคงเหลือ" className="rounded-xl border p-3 text-sm"/></div><textarea value={editingProduct.description} onChange={(event) => setEditingProduct({ ...editingProduct, description: event.target.value })} placeholder="รายละเอียดสินค้า" rows={4} className="w-full rounded-xl border p-3 text-sm"/><select value={editingProduct.status} onChange={(event) => { const status = event.target.value as ProductStatus; setEditingStatus(status); setEditingProduct({ ...editingProduct, status }); }} className="w-full rounded-xl border p-3 text-sm font-bold"><option value="AVAILABLE">พร้อมจำหน่าย</option><option value="DISCONTINUED">ยกเลิกจำหน่าย</option></select><button type="button" onClick={updateProduct} className="w-full rounded-xl bg-[#ef6c3d] px-4 py-3 font-bold text-white">บันทึกข้อมูลสินค้า</button></div>}<div className="grid grid-cols-2 gap-3"><div className="rounded-xl bg-[#f7f8f9] p-3"><p className="text-xs text-[#687582]">SKU</p><p className="mt-1 font-mono font-bold">{selectedProduct.sku}</p></div><div className="rounded-xl bg-[#f7f8f9] p-3"><p className="text-xs text-[#687582]">Category</p><p className="mt-1 font-bold">{selectedProduct.category}</p></div><div className="rounded-xl bg-[#f7f8f9] p-3"><p className="text-xs text-[#687582]">ราคา</p><p className="mt-1 font-bold">{formatBaht(selectedProduct.price)}</p></div><div className="rounded-xl bg-[#f7f8f9] p-3"><p className="text-xs text-[#687582]">คงเหลือ</p><p className="mt-1 font-bold">{selectedProduct.stock} ชิ้น</p></div><div className="rounded-xl bg-[#f7f8f9] p-3"><p className="text-xs text-[#687582]">สถานะสินค้า</p><select value={editingStatus} onChange={(event) => setEditingStatus(event.target.value as ProductStatus)} className="mt-1 w-full rounded-lg border bg-white p-2 text-sm font-bold"><option value="AVAILABLE">พร้อมจำหน่าย</option><option value="DISCONTINUED">ยกเลิกจำหน่าย</option></select><button type="button" onClick={updateProduct} className="mt-2 w-full rounded-lg bg-[#ef6c3d] px-3 py-2 text-xs font-bold text-white">บันทึกสถานะ</button></div></div><div><p className="font-bold">รายละเอียดสินค้า</p><p className="mt-2 whitespace-pre-wrap text-[#687582]">{selectedProduct.description || "ไม่มีรายละเอียด"}</p></div></div></div></div></div>}</>;
}
